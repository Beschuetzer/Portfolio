import { fontSizeFive, getFontSizeCustom } from "../../styles/constants";

export const INTER_SECTION_PADDING = getFontSizeCustom(4);
export const FONT_SIZE = fontSizeFive;
export const SIDE_PADDING = `calc(${FONT_SIZE} * 2)`;